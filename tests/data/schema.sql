CREATE TABLE users(id INTEGER PRIMARY KEY AUTOINCREMENT, facebook_id VARCHAR (255) NOT NULL, name VARCHAR (255) NOT NULL, email VARCHAR (255) NOT NULL, image VARCHAR (255), is_active INTEGER DEFAULT 1); CREATE TABLE access_token(id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INT NOT NULL, token VARCHAR (255) NOT NULL);